

#define INF (int)((1 << 31) - 1)
#define foreach(x,i) for(int i=0;i<x.size();i++)
#define io(v) cin>>v
#define toStr(Result) "Result ="
#define FUNCTION(m, op) void m(int& a,int b){if(b op a) {a=b;}}
